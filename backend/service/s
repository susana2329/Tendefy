        if (usuario.avatarUrl) {
            const avatar = await subirFotoCloudinary(usuario.avatarUrl,`${usuario.nombre}/perfil-usuario`)
            
            usuario.avatarUrl = avatar
        }
        if (usuario.fotos) {
            for (let i = 0; i < usuario.fotos.length; i++) {
                const foto = usuario.fotos[i]
                const cards = await subirFotoCloudinary(foto, `${usuario.nombre}/fotos-cards`)
                usuario.fotos[i] = cards
            }
        }
        console.log(usuario.avatarUrl)
        return result = await editProfileUpdate(usuario)